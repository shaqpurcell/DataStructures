#include <iostream>
#include <vector>

int removeDuplicates(std::vector<int>& nums);

int main() {
    std::cout << "Hello, World!" << std::endl;
    std::vector<int> nums;

    nums.push_back(1);
    nums.push_back(1);
    nums.push_back(4);
    nums.push_back(5);
    nums.push_back(6);
    nums.push_back(6);
    nums.push_back(6);

    removeDuplicates(nums);
    return 0;
}

int removeDuplicates(std::vector<int>& nums) {
    int uniqueIndex = 0;

    nums.siz

    }

}
